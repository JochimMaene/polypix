# Release notes

Polypix is pre-1.0. While it stays on `0.x`, breaking changes are preferred over
deprecation shims and compatibility layers: every release is tested and usable,
but names and signatures can still move. After 1.0 it follows semantic
versioning with normal deprecation periods.

```{include} ../CHANGELOG.md
:start-after: "# Changelog"
```
